example_template = executable('example-template', 'src/main.c',
  dependencies: [dependency('libseven'), dependency('minrt')],
  link_args: [mapfile.format('example-template')],
  name_suffix: 'elf')
